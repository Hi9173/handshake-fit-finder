{
  "name": "handshake-fit-finder",
  "private": true,
  "scripts": {
    "web:dev": "pnpm --dir apps/web dev",
    "web:build": "pnpm --dir apps/web build",
    "api:test": "cd apps/api && python3 -m unittest tests/test_scoring.py"
  }
}
